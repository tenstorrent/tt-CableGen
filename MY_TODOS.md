[] Adding connections, should have options to "add" at levels of hierarchy (starting from common parent to top-level)
[] Adding custom templates; add/instantiate empty graph template, and then move nodes into it (if moving from another template treat it as deleting that node then instantiating in the other one)
[] deleting node doesn't properly change the template connections; connections refrencing that node still exist in export
